import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function AnalyticsScreen() {
  return (
    <PlaceholderScreen
      title="Analytics"
      note="Category breakdown, month-over-month and projections land in plan 010."
    />
  );
}