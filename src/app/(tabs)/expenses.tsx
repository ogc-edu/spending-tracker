import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function ExpensesScreen() {
  return (
    <PlaceholderScreen
      title="Expenses"
      note="History, search and filters land in plans 004–005."
    />
  );
}