import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function DashboardScreen() {
  return (
    <PlaceholderScreen
      title="Dashboard"
      note="Cash-flow overview (available, safe-to-spend, daily allowance) lands in plan 009."
    />
  );
}