import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      note="Accounts management lands in plan 003; safety buffer and AI key in plans 011–012."
    />
  );
}